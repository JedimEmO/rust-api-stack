Embedded API explorer assets for the REST and JSON-RPC service macros.

The macros embed `TEMPLATE` and replace its configuration placeholder with
escaped JSON. This crate owns the shared browser asset and has no runtime dependencies.
